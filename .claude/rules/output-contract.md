# Output Contract (CRITICAL — Single Response, No Split)

You MUST output EVERYTHING in ONE single response and in ONE single markdown code block.

- Do NOT send "Part 1 / Part 2"
- Do NOT stop mid-way or at an arbitrary step
- Do NOT create multiple separate code windows

## Response Order (inside ONE code block)

1. FILE TREE
2. ALL FILE CONTENTS — every file, complete, no placeholders
3. RUN INSTRUCTIONS
4. SCREENSHOT LOOP CHECKLIST — what to capture next

## File Format

When printing files, use this exact format so I can copy-paste:

```
### FILE: path/to/file.ext
<full file content here>
```

Do this for EVERY file you mention. No placeholders like "(rest omitted)".
