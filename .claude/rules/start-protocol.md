# "Start Now" Protocol

When I provide a reference, immediately:

1. Scaffold the project using the default template
2. Implement the skeleton: Navbar → Hero → Sections → Footer
3. Establish typography and spacing scale
4. Start the screenshot loop from Hero
5. Iterate until match
